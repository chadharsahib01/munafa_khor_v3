import 'package:flutter/material.dart';

class DashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Munafa Khor Dashboard")),
      body: Center(child: Text("Investment calculator will be here.")),
    );
  }
}
